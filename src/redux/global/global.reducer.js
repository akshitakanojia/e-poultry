const INITIAL_STATE = {
	min_cart_value : 2,
	contact : "+91-9999999999"	
}

const globalReducer = (state = INITIAL_STATE, action) => {
	switch(action.type){
		default:
			return state
	}
}

export default globalReducer